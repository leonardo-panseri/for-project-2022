import mip


def sweep():
    pass


def cluster_first_route_second():
    # Translate all coordinates to have cartesian origin in the first location
    pass


def find_vehicle_paths(installed_markets, dist, x_coords, y_coords, truck_fixed_fee, truck_fee_per_km):
    model = cluster_first_route_second

    result = model()

    return result
